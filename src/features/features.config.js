/**
 * Feature configuration.
 *
 * This file is read by scripts/generate-features.js (Node runtime) and may also
 * be imported by app code.
 */

/** @type {{ enabled: string[] }} */
const config = {
  enabled: ['basic-pages']
};

export default config;
export { config };
